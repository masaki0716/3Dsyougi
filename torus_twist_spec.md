# 3つ目のシリーズ: ねじれトーラス盤 設計メモ

3Dsyougiの3つ目のバリエーション。torus.html(2つ目, 通常トーラス)をベースに、Z(段)方向を
「雑巾を絞る」ように1周ごとに1マスずらしながら完全ループさせる盤面。

## 合意済み方針

1. **ひねる軸**: Z(段)方向を完全ループ化し壁を撤去。1周(M段)するごとにθ(筋)が+1ずれる。
   θ方向自体はtorus.html同様シフト無しの単純な輪のまま。
2. **裏側の緩衝地帯**: 表側(中央、歩兵列同士の間)と対称に3段分の空きランクを追加する。
   - 盤の実マス: z=0..8 (9段、既存と同じ意味＝先手z=0/後手z=8)
   - 緩衝マス: z=9..11 (3段、駒なし・成りゾーンなし・通行は自由＝壁ではない)
   - Z主ループ全長 M = 12
3. **ジャンプ駒の境界越え**: 桂馬を含む全ての移動判定(steps/rays共通)で、一貫してねじれ座標変換
   `stepCell()` を通す。桂馬だけの特例は作らない。
4. **既存バグの横展開修正**: torus.htmlのレイ判定は「θ方向のみ(dz=0)のレイ」が発射地点に戻る
   終了条件を持たず、盤上に駒が1つも無い段(初期配置ではz=3,4,5)に駒が来た場合に理論上無限ループ
   する。torus.html・torus_twist.html の両方で「発射地点セルに戻ったら停止」を追加する。

## 座標変換の核 (torus_twist.html)

```
N = 9            // θ(筋)方向のマス数、不変
BOARD_Z = 9       // 実マス段数 z=0..8
BUFFER = 3        // 緩衝段数
M = BOARD_Z + BUFFER  // 12, Z主ループ全長
SHIFT = 1         // 1周ごとのθずらし量

tin(v) = ((v % N) + N) % N

// z2は任意の整数(桂馬のdz=+2で境界を跨ぐ場合など)。
// lap = 跨いだ周回数(整数、負もあり)。1周ごとにθをSHIFT分ずらす。
function stepCell(th, z, dth, dz){
  const z2 = z + dz;
  const lap = Math.floor(z2 / M);
  const nz = z2 - lap*M;             // 0..M-1
  const nth = tin(th + dth + lap*SHIFT);
  return {th: nth, z: nz};
}
```

- 全ての移動判定(歩・香・桂・銀・金・玉・飛・角とその成り)はこの`stepCell`を通す。
- レイ(飛・香・角・龍・馬)は1マスずつ`stepCell`を呼び、**発射地点セルに戻ったら停止**
  (`nxt.th===p.th && nxt.z===p.z`)。安全策として反復回数の上限 `N*M`(=108)も設ける。
  理論上、SHIFT=1・gcd(SHIFT,N)=1 なので、遮る駒が無ければ盤上の全108マス
  (実81+緩衝27)を1本のレイが1周してちょうど発射地点へ戻ってくる。
- 成りゾーン判定(`inZone`)は絶対z値(0-8)ベースを維持しつつ、緩衝マス(z>=9)を
  誤って含めないよう上限チェックを追加する: 先手 `z>=6 && z<=8` / 後手 `z>=0 && z<=2`。

## 見た目(3D形状)

- ゲームロジック上のθずれは境界(z=M-1→0)でだけ離散的に起きるが、見た目は境界だけで
  カクッと繋ぎ目が出ると不格好なので、`surfPoint`の角度計算にzに比例した連続的な
  ひねり項を加え、M段全体になだらかに1マス分(DTH)回転するスパイラル(雑巾絞り/ドリル刃状)
  の曲面として描画する。ゲーム内部の整数座標(th,z)には影響しない、純粋に描画上の調整。
- 緩衝マス(z=9..11)は盤と同じ曲面の続きとして描画し、壁の柵メッシュは廃止。
  木目テクスチャの色味を少し変える等で「何もない区間」と分かるようにする(任意)。

## 受け入れ条件

- [ ] 飛車をZ方向へ走らせ、障害物が無ければ最大108マス先(9周)まで移動候補になり、
      108マス目(元のマス)は候補に含まれない
- [ ] 上記のレイ列挙が有限回で終了する(ブラウザがフリーズしない)
- [ ] 緩衝マス(z=9..11)は初期配置で駒が無く、通常のマスとして移動・成りゾーン対象外で扱われる
- [ ] 桂馬がZ境界(z=M-1→0 または z=0→M-1)を跨ぐ移動でも、他の駒と同じ`stepCell`変換で
      正しいマスに着地する
- [ ] torus.htmlの「θのみのレイが発射地点に戻る」修正が入り、空の段でも無限ループしない
- [ ] index.html / torus.html の挙動は変更しない(torus.htmlはバグ修正のみ)

## ファイル

- 新規: `3Dsyougi/torus_twist.html` (torus.htmlをベースに複製・改修)
- 修正: `3Dsyougi/torus.html` (レイ終了条件のみ)

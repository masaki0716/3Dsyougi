# 立体将棋 オンライン対戦 — サーバー起動・終了手順

同じWiFi内の相手に `index.html` を直接URLで開いてもらうための、PC側の簡易サーバー手順。
（対局そのもののマッチングは対局コードで行うので、この手順は「相手の画面にゲームを表示させる」ためのもの）

**重要: 必ずHTTPS（`https://`）で配信すること。** 対局コードでのマッチング機能（Trystero）はブラウザの Web Crypto API を使っており、これは `localhost` 以外では HTTPS でないと動作しない（`crypto.subtle` が `undefined` になり、対局相手を待ったまま永久に固まる）。プレーンな `http://192.168.x.x:...` で配信すると、コードが完全に一致していてもマッチングが成立しない。

## 全体の流れ

1. PCで自己署名証明書を用意する（初回のみ）
2. PCでHTTPSサーバーを起動する
3. PCのIPアドレスを確認する
4. 相手にURL（`https://`）を伝える（相手は**PCと同じWiFi**に接続している必要がある。モバイルデータ不可）
5. 初回アクセス時、双方のブラウザで証明書の警告を「許可」する（自己署名証明書のため。1回だけ）
6. お互いに `index.html` の画面上で「誰かと対戦」→**対局コード**を伝え合って対局開始
7. 終わったらサーバーを終了する

---

## 1. 証明書の準備（初回のみ）

`3Dsyougi\_cert\` フォルダに `cert.pem` / `key.pem` が無ければ、Git Bashで以下を実行（作成済みなら不要）。

```bash
mkdir -p "C:\Users\masas\knowledgeBaseSystem\3Dsyougi\_cert"
cd "C:\Users\masas\knowledgeBaseSystem\3Dsyougi\_cert"
MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL="*" openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 3650 -nodes \
  -subj "/CN=3dsyougi.local" \
  -addext "subjectAltName=IP:<PCのIP>,IP:127.0.0.1,DNS:localhost"
```

PCのIPアドレスが変わった場合（下記2で確認）、SANに含まれていないと警告が余分に出ることがあるので作り直す（動作自体には影響しない）。

## 2. サーバー起動

PowerShellかコマンドプロンプトで:
```
cd C:\Users\masas\knowledgeBaseSystem\3Dsyougi
npx --yes http-server . -S -C "_cert/cert.pem" -K "_cert/key.pem" -p 8443 -a 0.0.0.0
```

初回起動時にWindowsファイアウォールの確認ダイアログが出たら「アクセスを許可する」を選ぶ。

## 3. PCのIPアドレス確認

PowerShellで:
```
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -like "*Wi-Fi*" } | Select-Object InterfaceAlias, IPAddress
```
`192.168.x.x` のようなアドレスが出る（WiFi再接続のたびに変わることがあるので、繋がらない時は再確認する）。

## 4. 相手に伝えるURL

```
https://<↑で確認したIP>:8443/index.html
```

**相手はPCと同じWiFi(SSID)に接続していること**（モバイルデータ/別のWiFiでは物理的に届かない）。

## 5. 証明書の警告を許可（初回のみ・双方の端末で）

自己署名証明書なので、開くと「保護されていない通信」「プライバシーエラー」等の警告画面が出る。これは想定通り。
「詳細設定」→「〜にアクセスする（安全ではありません）」を選んで進む。一度許可すればそのブラウザでは以後出ない。

## 6. 対局コードでマッチング

双方の画面で「誰かと対戦（オンライン）」を選択。片方の画面に出る5文字の**対局コード**をもう片方に伝えて同じコードで参加すると、自動でマッチングして先手/後手が決まり対局開始。
マッチング自体はインターネット経由（Trystero）なので、WiFiがネット接続されていれば通常通り動く。

---

## 終了手順

サーバーを起動したターミナルで **Ctrl + C**、またはそのウィンドウを閉じる。
これでPC以外の端末からは `index.html` にアクセスできなくなる。

---

## トラブルシューティング

| 症状 | 原因・対処 |
|---|---|
| 対局コードが一致してるのに「対戦相手を待っています…」から進まない | **最頻出の原因。** `http://` で配信していないか確認（`crypto.subtle` が使えず内部で例外が出て失敗する）。必ず `https://` + 上記の証明書警告を許可した状態で開くこと |
| スマホで白い画面のまま動かない | スマホがモバイルデータ接続になっている可能性が高い。WiFi設定でPCと同じSSIDに繋ぎ直す |
| URLを開いても繋がらない | PCのIPアドレスが変わっていないか再確認（`Get-NetIPAddress`）。サーバーを起動し忘れていないかも確認 |
| 初回だけ繋がらない | Windowsファイアウォールの許可ダイアログが出ていたら「許可」を選ぶ（キャンセルすると以後ブロックされる） |
| ブラウザで証明書エラーのまま先に進めない | 「詳細設定」的なリンクが警告文の下に小さく出ているはず。無ければ画面を少しスクロールしてみる |

# 公開・収益化リスク検討（2026-07-25）

立体将棋（`index.html`）を一般公開し、アフィリエイト広告・スマホアプリ広告（AdMob等）で収益化することを検討するにあたり、洗い出したリスク・制約のメモ。方向性はまだ未決定、まずリスクの棚卸しのみ。

## A. 技術的な依存関係

- **Trystero**: MITライセンスで商用利用は問題なし。デフォルトのNostr戦略はREADME上「数百の稼働リレーがあり冗長性が高い」とされているが、正式なSLA・稼働保証はない。将来的にスケールが必要になったらSupabase/Firebaseのセルフホスト戦略への切替も選択肢（README側もそれを「中間案」として推奨）。
- **CDN依存**（three.js@cdnjs、jsdelivr、esm.run）: いずれも無料・商用利用可だが単一障害点でSLAなし。本番公開するなら自前バンドル配信への切替を検討する価値あり。
  - **2026-07-27対応（B-4、`claude/tech-polish`）**: three.js本体・OrbitControls・LineSegments2系（計5ファイル、合計約650KB）は`vendor/three/`配下に自前ホスティングに切替済み（`<script src>`をCDN URLから相対パスへ変更）。いずれもESM importを使わないクラシックスクリプト（グローバルTHREE依存）で、依存の連鎖がないため安全に切替できた。
  - **Trysteroは現状維持（許容）と判断**: `https://esm.run/trystero`が返すファイルはjsdelivr上で`@trystero-p2p/nostr@0.25.3`→`@trystero-p2p/core@0.25.3`・`@noble/secp256k1@3.1.0`と多段に`export*from"..."`で再エクスポートする動的バンドルになっており、自前ホスティングするには依存グラフ全体を手動で追跡・ダウンロードしローカルパスへ書き換える作業が必要（実質的にビルドツールを自作するのに近い）。単一HTML・ビルドツール非導入の方針とトレードオフが合わないため見送り、CDN依存として許容する。

## B. IPリスク（最重要・要判断）

- `index.html:648` の撃破演出で、王を取った時のテキストが **「ザ・ワールド！」**（JoJoのスタンド名そのまま）になっている。ローカルの身内利用では実害なしで許容してきたが、**広告で収益化して一般公開**するとなると話が変わる。収益が絡むと権利者側に申し立てのインセンティブが生まれるため、著作権/商標リスクが上がる典型パターン。
- 他のBOOM_WORDS（メメタァ 等、`index.html:603`）も既存作品由来の可能性があり、公開前に一通り洗い出して置き換えるか判断が必要。
- 一方、駒テクスチャはcanvas自前生成、three.jsはMIT — この2つは問題なし。

## C. 広告収益化まわりのポリシー制約

- **AdSense**: プライバシーポリシーページが実質必須。EU/UK訪問者にはCookie同意(CMP)がGoogleの「EUユーザー同意ポリシー」上ほぼ必須。単一ページの静的ゲームは「独自コンテンツの量」の面で審査が渋いことがある（通る場合もあるが保証はない）。
- **スマホアプリ化(AdMob)**: 2026年時点でGoogle Play・App Store双方とも「WebViewラップだけのアプリ」への審査が厳格化。プッシュ通知・オフライン対応・ネイティブUIなど「アプリならではの価値」がないと却下されやすい（単純ラップ＋広告だけの構成はリスクが高い）。Google Play買切$25／Apple年$99の登録費も要る。

## D. オンライン対戦特有の論点

- 現状の実装は「身内でコードを共有」前提（5文字コードに3人目が来たら無視＝早い者勝ち、`index.html:759`）。不特定多数が使う公開ゲームになると、コード衝突・荒らし・切断対応などの設計を見直す必要が出てくる。

## E. その他（法的リスクではないが体験上の懸念）

- 筆文字フォントは埋め込みでなくシステムフォント依存（Yu Mincho等）。Windows以外やモバイルでは意図した見た目にならずMSゴシック/汎用serifにフォールバックする。
  - **2026-07-27調査（B-5、`claude/tech-polish`）**: 対応不要（現状維持）と判断。
    - Yu Mincho自体はWindows同梱のプロプライエタリフォントで再配布不可のため、代替として無料ライセンス（OFL）の筆文字系Webフォント「Yuji Syuku」（Google Fonts）の自前ホスティングを検討したが、フルセット（全字形込み）のTTFで**7.8MB**あり、単一HTML・軽量構成の方針と大きく矛盾する
    - ゲーム内で使用する文字（駒の漢字15種＋UI文言）だけに絞ったサブセット化（fonttools等）をすれば数十KB程度まで縮小できる可能性はあるが、ビルドツール非導入の方針下では手作業でのサブセット生成・再生成が必要になり、UI文言を追加するたびにメンテナンスコストが発生する
    - Google Fonts CDN経由での配信（サブセット不要）という代替案は、B-4でCDN依存を減らす方向と矛盾する上、B-2で検討予定のCookie同意・プライバシー配慮（Googleサーバーへの通信が発生する）とも相性が悪い
    - 以上より、フェーズ0（実験的検証）では「Windows以外はフォールバックフォントで見た目が変わる」ことを許容し、反応を見てから（本格運用に進む場合に）再検討する
- ドメイン: 無料サブドメインで始めるか、独自ドメイン取得するか。

## 次に深掘りできる項目（未着手）

- BOOM_WORDS全部の出典確認・代替案
- AdSense審査の実際のハードル感
- TWA/Capacitorでの必要最小構成の具体化
